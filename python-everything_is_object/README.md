# python-everything_is_object

This project explores how Python objects work under the hood: types,
identifiers (`id()`), mutability vs immutability, and the difference
between `==` (equality) and `is` (identity) for integers, strings,
lists, and tuples.

## Tasks

| File | Description |
| --- | --- |
| 0-answer.txt | Function used to print the type of an object |
| 1-answer.txt | Function used to get the variable identifier |
| 2-answer.txt | Same object? `a = 89`, `b = 100` |
| 3-answer.txt | Same object? `a = 89`, `b = 89` |
| 4-answer.txt | Same object? `a = 89`, `b = a` |
| 5-answer.txt | Same object? `a = 89`, `b = a + 1` |
| 6-answer.txt | `s1 == s2` after `s2 = s1` |
| 7-answer.txt | `s1 is s2` after `s2 = s1` |
| 8-answer.txt | `s1 == s2` for two equal string literals |
| 9-answer.txt | `s1 is s2` for two equal string literals |
| 10-answer.txt | `l1 == l2` for two equal lists |
| 11-answer.txt | `l1 is l2` for two equal lists |
| 12-answer.txt | `l1 == l2` after `l2 = l1` |
| 13-answer.txt | `l1 is l2` after `l2 = l1` |
| 14-answer.txt | Effect of `l1.append()` on an aliased list |
| 15-answer.txt | Effect of `l1 = l1 + [...]` on an aliased list |
| 16-answer.txt | Passing an int to a function (pass by value) |
| 17-answer.txt | Passing a list to a function (pass by reference) |
| 18-answer.txt | Reassigning a parameter inside a function |
| 19-copy_list.py | Function that returns a copy of a list |
| 20-answer.txt to 23-answer.txt | Is it a tuple? |
| 24-answer.txt to 26-answer.txt | Identity of equal tuples |
| 27-answer.txt | Does `id()` stay the same after `a = a + [...]`? |
| 28-answer.txt | Does `id()` stay the same after `a += [...]`? |
