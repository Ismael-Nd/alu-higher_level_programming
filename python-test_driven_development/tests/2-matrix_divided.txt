The ``2-matrix_divided`` module
=================================

Import the function:

    >>> matrix_divided = __import__('2-matrix_divided').matrix_divided

Divide a matrix of integers:

    >>> matrix = [
    ...     [1, 2, 3],
    ...     [4, 5, 6]
    ... ]
    >>> matrix_divided(matrix, 3)
    [[0.33, 0.67, 1.0], [1.33, 1.67, 2.0]]

The original matrix is not modified:

    >>> matrix
    [[1, 2, 3], [4, 5, 6]]

Divide a matrix of floats:

    >>> matrix_divided([[1.5, 2.5], [3.5, 4.5]], 2)
    [[0.75, 1.25], [1.75, 2.25]]

matrix must be a list of lists:

    >>> matrix_divided([1, 2, 3], 2)
    Traceback (most recent call last):
    TypeError: matrix must be a matrix (list of lists) of integers/floats

    >>> matrix_divided("hello", 2)
    Traceback (most recent call last):
    TypeError: matrix must be a matrix (list of lists) of integers/floats

matrix elements must be integers or floats:

    >>> matrix_divided([[1, "a"], [3, 4]], 2)
    Traceback (most recent call last):
    TypeError: matrix must be a matrix (list of lists) of integers/floats

Each row must have the same size:

    >>> matrix_divided([[1, 2], [3, 4, 5]], 2)
    Traceback (most recent call last):
    TypeError: Each row of the matrix must have the same size

div must be a number:

    >>> matrix_divided([[1, 2], [3, 4]], "a")
    Traceback (most recent call last):
    TypeError: div must be a number

div cannot be zero:

    >>> matrix_divided([[1, 2], [3, 4]], 0)
    Traceback (most recent call last):
    ZeroDivisionError: division by zero

Dividing by float("inf") makes every value 0.0:

    >>> matrix_divided([[1, 2], [3, 4]], float("inf"))
    [[0.0, 0.0], [0.0, 0.0]]

A matrix element that is float("inf") or float("nan") divides normally:

    >>> matrix_divided([[float("inf"), 2], [3, 4]], 2)
    [[inf, 1.0], [1.5, 2.0]]

    >>> matrix_divided([[float("nan"), 2], [3, 4]], 2)
    [[nan, 1.0], [1.5, 2.0]]

A matrix element that is float("inf") or float("nan") divided by
float("inf") or float("nan"):

    >>> matrix_divided([[float("inf"), 2], [3, 4]], float("inf"))
    [[nan, 0.0], [0.0, 0.0]]

    >>> matrix_divided([[float("nan"), 2], [3, 4]], float("nan"))
    [[nan, nan], [nan, nan]]

Calling with a missing required argument raises a TypeError:

    >>> matrix_divided([[1, 2], [3, 4]])
    Traceback (most recent call last):
    TypeError: matrix_divided() missing 1 required positional argument: 'div'

Calling with both required arguments missing raises a TypeError:

    >>> matrix_divided()
    Traceback (most recent call last):
    TypeError: matrix_divided() missing 2 required positional arguments: 'matrix' and 'div'
